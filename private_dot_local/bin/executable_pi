#!/usr/bin/env bash
MISE="$(command -v mise || echo /opt/homebrew/bin/mise)"
NODE="$("$MISE" where node@26)"
exec "$NODE/bin/node" \
    "$NODE/lib/node_modules/@earendil-works/pi-coding-agent/dist/cli.js" "$@"
