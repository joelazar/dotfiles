# generated by `tv migrate-config`, original backed up as config.toml.bak

[shell_integration.channel_triggers]
dirs = [
    "cd",
    "ls",
    "rmdir",
    "rm -rf",
]
git-branch = [
    "g co",
    "git checkout",
    "git branch",
    "git merge",
    "git rebase",
    "git pull",
    "git push",
]
git-repos = [
    "nvim",
    "code",
    "hx",
    "zed",
    "git clone",
]
ssh-hosts = [
    "ssh",
    "scp",
    "rsync",
]
text = [
    "rg",
    "grep",
]

[ui]
theme = "catppuccin-mocha"
