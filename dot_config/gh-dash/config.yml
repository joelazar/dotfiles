prSections:
  - title: My Pull Requests
    filters: is:open author:@me
  - title: Needs My Attention
    filters: is:open -author:@me involves:@me -label:wip -is:draft
  - title: Work in progress
    filters: is:open -author:@me user:Growblocks label:wip
  - title: Only work
    filters: is:open -author:@me user:Growblocks
  - title: Recently closed
    filters: is:closed user:Growblocks
issuesSections:
  - title: My Issues
    filters: is:open author:@me
  - title: Assigned
    filters: is:open assignee:@me
  - title: Subscribed
    filters: is:open -author:@me repo:cli/cli repo:dlvhdr/gh-dash
defaults:
  preview:
    open: true
    width: 60
  prsLimit: 50
  issuesLimit: 20
  view: prs
  refetchIntervalMinutes: 10
repoPaths:
  Growblocks/*: ~/git/work/*
pager:
  diff: delta
keybindings:
  prs:
    - key: v
      command: >
        kitty @ launch --type=tab --tab-title "👷 {{.RepoName}} - PR {{.PrNumber}}" --keep-focus
        fish -c 'cd {{.RepoPath}} &&
        gh pr checkout {{.PrNumber}} &&
        nvim'
    - key: V
      command: >
        kitty @ launch --type=tab --tab-title "📖 {{.RepoName}} - PR {{.PrNumber}}" --keep-focus
        fish -c 'cd {{.RepoPath}} && nvim -c ":let g:ministarter_disable = v:true | Octo pr edit {{.PrNumber}}"'
    - key: D
      command: >
        cd {{.RepoPath}} &&
        gh pr checkout {{.PrNumber}} &&
        nvim -c ":DiffviewOpen main...{{.HeadRefName}}"
    - key: O
      command: >
        gh pr review --repo {{.RepoName}} {{.PrNumber}} --approve
    - key: m
      command: >
        gh pr merge --repo {{.RepoName}} {{.PrNumber}} --rebase --delete-branch
    - key: M
      command: >
        gh pr merge --repo {{.RepoName}} {{.PrNumber}} --admin --rebase --delete-branch
