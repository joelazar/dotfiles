---
description: Search the web for given query
argument-hint: <query>
---

Use the `web-search` skill to find: $@

Start at tier 1 (kagi quick) and escalate through the skill's defined tier order only if the answer is insufficient. Do NOT use the `kagi`, `perplexity-search`, or `ai-search` skills directly unless I explicitly name a provider.
